{
  "name": "harmful-tower-guard",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "init-weapons": "node scripts/initWeapons.js",
    "check-deploy": "node scripts/checkDeploy.js",
    "deploy": "npm run check-deploy && npm run build && firebase deploy --only hosting",
    "deploy:preview": "npm run build && firebase hosting:channel:deploy preview",
    "get-config": "node scripts/getFirebaseConfig.js",
    "set-admin": "node scripts/setAdmin.js",
    "check-rules": "node scripts/checkFirestoreRules.js"
  },
  "dependencies": {
    "firebase": "^12.6.0",
    "firebase-admin": "^13.6.0",
    "vue": "^3.5.25"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.2",
    "terser": "^5.44.1",
    "vite": "^7.2.4",
    "vite-plugin-vue-devtools": "^8.0.5"
  }
}
